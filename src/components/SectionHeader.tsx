interface SectionHeaderProps {
  tag: string;
  title: string;
  description: string;
  nowrap?: boolean;
}
const SectionHeader = ({
  tag,
  title,
  description,
  nowrap,
}: SectionHeaderProps) => {
  return <div></div>;
};

export default SectionHeader;
